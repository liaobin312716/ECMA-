## 语言概览

ECMAScript 是一门基于对象的语言，一个 ECMAScript 对象是属性和方法的集合。
ECMAScript 也提供了很多内置对象，Object, Function, Array, String, Boolean, Math, RegExp ...

## 4.2.1对象

ECMAScript 没有类的概念，而是通过对构造函数执行 new 操作创建，构造函数是一个具有原型属性的函数， 原型通常被用于原型继承和共享属性&方法。

##  4.2.2 严格模式

## 4.3 名词解释
